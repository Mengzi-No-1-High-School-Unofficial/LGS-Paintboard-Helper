针对绘板组织方近日引入了 Token CD 机制，而我们之前的代码设计是针对无 CD 方案进行的。

原来的方案，大量发送包并通过速率限制、粘包机制以避免触发 IP RateLimit。而现在主要的限制已经变为针对 Token 的限制（一个 Token 每个 CD 时间只能绘画一个像素点），我们有必要重新设计一个新的方案。

1. 首先，我们必须允许用户手动配置 CD 时间
2. 其次，我们必须允许多 Token 机制，以换取更多的绘制机会
3. 并且，还要抛弃粘包，这会导致多 Token 机制实现的复杂

其他大抵的机制参照增量模式设计（优先级绘制等），但是取消初始绘制，而是不断比对本地绘板和目标数据，加入优先队列中。然后通过 Producer Customer 架构，维护多个 Token 实例不断从队列中取出像素点，如果自己能绘画（不在 CD 间期内）就直接绘制。

对于每次新的比对，我们必须清除优先队列中的数据，以确保即时性。

绘制完成后，我们要手动更新 LocalBoard。据我所知，当前 LocalBoard 自带的事件监听机制并不会处理本地绘制事件（这一点请你再次确认）。

原先的 `paint` 函数不支持临时 Token 身份认证，不断调用全局的 `set_auth` 可能导致并发错误。因此需要设计一个新的 `paint` 函数。

对于用户交互方面：

1. 此模式必须与增量模式并行存在
2. 我们允许用户通过配置文件和命令行两种方式导入 AccessKey + UID，然后自动换到 Token